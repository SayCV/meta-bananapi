diff --git a/Makefile b/Makefile
index ca20c12..0001de1 100644
--- a/Makefile
+++ b/Makefile
@@ -934,7 +934,7 @@ cmd_pad_cat = $(cmd_objcopy) && $(append) || rm -f $@
 all:		$(ALL-y)
 ifndef BUILD_BOOTCODE_ONLY
 	$(MAKE) -C $(SUBDIR_EXAMPLES) all PRJ=$(PRJ) CUSTOMER_ID=$(CUSTOMER_ID)\
-	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC=$(CC) LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
+	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC="$(CC)" LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
 #	$(MAKE) -C $(SUBDIR_EFUSE_EXAMPLES) all PRJ=$(PRJ) CUSTOMER_ID=$(CUSTOMER_ID)\
 #	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC=$(CC) LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
 #	$(MAKE) -C $(SUBDIR_EFUSE_VERIFY_EXAMPLES) all PRJ=$(PRJ) CUSTOMER_ID=$(CUSTOMER_ID)\
@@ -999,7 +999,6 @@ u-boot.bin: u-boot FORCE
 	$(call if_changed,objcopy)
 	$(call DO_STATIC_RELA,$<,$@,$(CONFIG_SYS_TEXT_BASE))
 	$(BOARD_SIZE_CHECK)
-	@cp -v $@ $(objtree)/../rtk-pack/rtk/bpi-w2/bin/$@
 
 u-boot.ldr:	u-boot
 		$(CREATE_LDR_ENV)
@@ -1583,9 +1582,9 @@ clean: $(clean-dirs) clean-efuse-dir clean-flush-writer-dir
 
 clean-efuse-dir:
 	$(MAKE) -C $(SUBDIR_EFUSE_EXAMPLES) clean PRJ=$(PRJ) CUSTOMER_ID=$(CUSTOMER_ID)\
-	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC=$(CC) LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
+	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC="$(CC)" LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
 	$(MAKE) -C $(SUBDIR_EFUSE_VERIFY_EXAMPLES) clean PRJ=$(PRJ) CUSTOMER_ID=$(CUSTOMER_ID)\
-	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC=$(CC) LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
+	 CHIP_ID=$(CHIP_ID) CHIP_TYPE=$(CHIP_TYPE) FLASH_TYPE=$(FLASH_TYPE) CC="$(CC)" LD=$(LD) OBJCOPY=$(OBJCOPY) OBJDUMP=$(OBJDUMP)
 
 clean-flush-writer-dir:
 	$(MAKE) -C $(SUBDIR_EXAMPLES) clean PRJ=$(PRJ) CUSTOMER_ID=$(CUSTOMER_ID)
-- 
2.7.4

